import React from 'react';

function AppBar(props) {
    return (
        <nav className="container-fluid text-center h-100 w-100 app-bar ">
            AppBar Component
        </nav>
    );
}

export default AppBar;